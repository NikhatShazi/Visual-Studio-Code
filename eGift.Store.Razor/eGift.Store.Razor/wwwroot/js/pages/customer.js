//Page load.$(document).ready(function () {    CalculateAge();    CalculateAgeText();    $("#CustomerViewModel_DateOfBirth").change(function () {        CalculateAge();    });    function CalculateAge() {        var dob = $("#CustomerViewModel_DateOfBirth").val();        dob = new Date(dob);        var today = new Date();        var age = Math.floor((today - dob) / (365.25 * 24 * 60 * 60 * 1000));        $('#CustomerViewModel_Age').val(age);    }    function CalculateAgeText() {        var dob = $("#DateOfBirth").text();        dob = new Date(dob);        var today = new Date();        var age = Math.floor((today - dob) / (365.25 * 24 * 60 * 60 * 1000));        $('#Age').text(age);    }    //To upload/change profile-image    $("#CustomerViewModel_ProfileImage").change(function () {        var fileReader = new FileReader();        fileReader.readAsDataURL($("#CustomerViewModel_ProfileImage").get(0).files[0]);        fileReader.onload = function (event) {            $("#profile-image-preview").attr("src", event.target.result);            $("#profile-image-validtion").addClass('d-none');        };    });    // To clear profile-image    $("#clear-profile-image").click(function () {        $("#CustomerViewModel_ProfileImage").replaceWith($("#CustomerViewModel_ProfileImage").val('').clone(true));        $("#profile-image-preview").attr("src", "/Images/default-file.png");    });});